document.getElementById('doctor-registration-form').addEventListener('submit', function(event) {
    // Validate form data and handle the registration logic
    event.preventDefault();
    alert('Doctor registered!');
});
